## Sassy v2 benchmarks

### Running benchmarks
For figure 1 in the paper we have panel A which tests scaling with different text lengths,
and panel B which tracks scaling (and [IPC](https://en.wikipedia.org/wiki/Instructions_per_cycle)) with increasing number of patterns. The benchmark
and figure creation can be ran directly using `just sassy2_fig1`, without just
you can copy the steps from the [justfile](/justfile)